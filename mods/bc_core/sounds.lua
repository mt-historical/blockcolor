--
-- Sounds
--

function bc_core.sound(table)
	table = table or {}
	table.footstep = table.footstep or
		{ name = "default_footstep_blockcolor", gain = 1.0 }
	table.dig = table.dig or
		{ name = "default_dig_blockcolor", gain = 0.25 }
	table.dug = table.dug or
		{ name = "default_dug_blockcolor", gain = 0.25 }
	table.place = table.place or
		{ name = "default_place_blockcolor", gain = 1.0 }
	return table
end

function bc_core.sound_water(table)
	table = table or {}
	table.footstep = table.footstep or
		{ name = "default_footstep_blockcolor", gain = 1.0 }
	table.dig = table.dig or
		{ name = "default_dig_blockcolor", gain = 0.25 }
	table.dug = table.dug or
		{ name = "default_dug_blockcolor", gain = 0.25 }
	table.place = table.place or
		{ name = "default_place_blockcolor", gain = 1.0 }
	return table
end

function bc_core.sound_wood(table)
	table = table or {}
	table.footstep = table.footstep or
		{ name = "default_footstep_blockcolor", gain = 1.0 }
	table.dig = table.dig or
		{ name = "default_dig_blockcolor", gain = 0.25 }
	table.dug = table.dug or
		{ name = "default_dug_blockcolor", gain = 0.25 }
	table.place = table.place or
		{ name = "default_place_blockcolor", gain = 1.0 }
	return table
end

function bc_core.sound_glass(table)
	table = table or {}
	table.footstep = table.footstep or
		{ name = "default_footstep_blockcolor", gain = 1.0 }
	table.dig = table.dig or
		{ name = "default_dig_blockcolor", gain = 0.25 }
	table.dug = table.dug or
		{ name = "default_dug_blockcolor", gain = 0.25 }
	table.place = table.place or
		{ name = "default_place_blockcolor", gain = 1.0 }
	return table
end

function bc_core.sound_stone(table)
	table = table or {}
	table.footstep = table.footstep or
		{ name = "default_footstep_blockcolor", gain = 1.0 }
	table.dig = table.dig or
		{ name = "default_dig_blockcolor", gain = 0.25 }
	table.dug = table.dug or
		{ name = "default_dug_blockcolor", gain = 0.25 }
	table.place = table.place or
		{ name = "default_place_blockcolor", gain = 1.0 }
	return table
end

function bc_core.sound_leaves(table)
	table = table or {}
	table.footstep = table.footstep or
		{ name = "default_footstep_blockcolor", gain = 1.0 }
	table.dig = table.dig or
		{ name = "default_dig_blockcolor", gain = 0.25 }
	table.dug = table.dug or
		{ name = "default_dug_blockcolor", gain = 0.25 }
	table.place = table.place or
		{ name = "default_place_blockcolor", gain = 1.0 }
	return table
end

function bc_core.sound_metal(table)
	table = table or {}
	table.footstep = table.footstep or
		{ name = "default_footstep_blockcolor", gain = 1.0 }
	table.dig = table.dig or
		{ name = "default_dig_blockcolor", gain = 0.25 }
	table.dug = table.dug or
		{ name = "default_dug_blockcolor", gain = 0.25 }
	table.place = table.place or
		{ name = "default_place_blockcolor", gain = 1.0 }
	return table
end
